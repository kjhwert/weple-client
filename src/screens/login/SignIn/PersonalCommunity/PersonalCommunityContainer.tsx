import React from 'react';
import PersonalCommunityPresenter from './PersonalCommunityPresenter';

export default ({navigation}) => {
  return <PersonalCommunityPresenter navigation={navigation} />;
};
