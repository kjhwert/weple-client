import React from 'react';
import TogetherPostIntroducePresenter from './TogetherPostIntroducePresenter';

interface IProps {
  navigation: any;
}

export default ({navigation}: IProps) => {
  return <TogetherPostIntroducePresenter navigation={navigation} />;
};
