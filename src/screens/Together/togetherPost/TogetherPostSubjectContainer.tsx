import React from 'react';
import TogetherPostSubjectPresenter from './TogetherPostSubjectPresenter';

interface IProps {
  navigation: any;
}

export default ({navigation}: IProps) => {
  return <TogetherPostSubjectPresenter navigation={navigation} />;
};
