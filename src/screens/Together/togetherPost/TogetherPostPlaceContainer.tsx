import React from 'react';
import TogetherPostPlacePresenter from './TogetherPostPlacePresenter';

interface IProps {
  navigation: any;
}

export default ({navigation}: IProps) => {
  return <TogetherPostPlacePresenter navigation={navigation} />;
};
