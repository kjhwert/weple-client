import React from 'react';
import SetAskDetailPresenter from './SetAskDetailPresenter';

interface IProps {
  navigation: any;
}

export default ({navigation}: IProps) => {
  return <SetAskDetailPresenter navigation={navigation} />;
};
