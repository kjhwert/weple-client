import React from 'react';
import SetAskWritePresenter from './SetAskWritePresenter';

interface IProps {
  navigation: any;
}

export default ({navigation}: IProps) => {
  return <SetAskWritePresenter navigation={navigation} />;
};
